import { Metadata, Viewport } from 'next';

export const metadata: Metadata = {
  title: 'Sign In - CRM Suite',
  description: 'Sign in to your CRM Suite account',
};

export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
}; 